{% extends 'base.html.twig' %}

{% block body %}
  <table class="table">

		<thead>
			<tr>
				<th scope="col">#</th>
				<th scope="col">Nom</th>
				<th scope="col">description</th>
				<th scope="col">taille</th>
				<th scope="col">poid</th>
        	<th scope="col">famille</th>
				<th scope="col">Action</th>
			</tr>
		</thead>
    <tbody>
			{% for produit in produitList %}
				<tr>
					<th scope="row">produit.id</th>
					<td>{{ produit.Nom }}</td>
					<td>{{ produit.description }}</td>
					<td>{{ produit.taille }}</td>
					<td>{{ produit.poid }}</td>
					<td>{{ produit.famille }}</td>
					<th scope="col">
          <a href="{{ path('app_catalog', {id: produit.id}) }}" type="button" class="btn btn-info">Afficher</a>

						{# {% if app.user %} #}
							<a href="{{ path('app_modifier', {id: produit.id}) }}" type="button" class="btn btn-warning">Modifier</a>
							<a href="{{ path('app_supprimer', {id: produit.id}) }}" type="button" class="btn btn-danger">Supprimer</a>
              <a href="{{ path('app_ajouter', {id: produit.id}) }}" type="button" class="btn btn-success">Ajouter</a>
						{# {% endif %} #}
      {% endfor %}
  
  
{% endblock %}